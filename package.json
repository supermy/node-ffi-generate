{
  "name": "ffi-generate",
  "version": "v1.0.0",
  "description": "Generate FFI Bindings from header file",
  "keywords": [
    "libclang",
    "ffi",
    "bindings"
  ],
  "author": "Timothy J Fontaine <tjfontaine@gmail.com>",
  "contributors": [
    {
      "name": "Joel Purra",
      "email": "code+npm@joelpurra.com",
      "url": "https://joelpurra.com/"
    }
  ],
  "main": "lib/generateffi.js",
  "license": "MIT",
  "engines": {
    "node": ">=10.0.0"
  },
  "bin": {
    "ffi-generate": "bin/ffi-generate.js"
  },
  "dependencies": {
    "engine-check": "^1.0.1",
    "hogan.js": ">= 2.0.0",
    "js-beautify": "",
    "libclang": "github:node-ffi-packager/node-libclang#semver:^v1.0.0",
    "optimist": ""
  },
  "devDependencies": {
    "ffi-napi": "^3.0.1",
    "husky": "^4.3.0",
    "prettier": "^2.0.5",
    "ref-array-di": "^1.2.2",
    "ref-napi": "^2.1.2",
    "ref-struct-di": "^1.1.1",
    "ref-union-di": "^1.0.1"
  },
  "peerDependencies": {
    "ffi-napi": "^3.0.1",
    "ref-array-di": "^1.2.2",
    "ref-napi": "^2.1.2",
    "ref-struct-di": "^1.1.1",
    "ref-union-di": "^1.0.1"
  },
  "scripts": {
    "test": "npm run --silent lint",
    "lint": "npm run --silent lint:prettier",
    "lint:fix": "npm run --silent lint:prettier:fix",
    "lint:prettier": "prettier --list-different . || { echo \"Prettier needs to format the above files. Try 'npm run --silent lint:fix'.\" && exit 1; }",
    "lint:prettier:fix": "prettier --write ."
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run --silent test",
      "pre-push": "npm run --silent test"
    }
  }
}
